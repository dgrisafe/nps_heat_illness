# set directories

# call home directories, hidden from Git
source("directories_home.R") # dir_home <- 

# assign project subfolders
dir_data <- paste0(dir_home, "\\0 Data")

# dir_programs <- # assigned in separate folder on local machine
dir_reports <- paste0(dir_home, "\\3 Reports")
